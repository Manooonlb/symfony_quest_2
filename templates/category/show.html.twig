{% extends 'base.html.twig' %}


{% block title %}Les séries de la catégorie {{ category.name }} {% endblock %}

{% block body %}
<h1>Les séries de la catégorie {{ category.name }}</h1>
    {% for program in programs %}
        <div>
            <h1>{{program.id}} {{program.title}}</h1>
            <br>
            <p>{{program.synopsis}}</p>
            {# Lien vers la fiche de la série : #}
            <a href="{{ path("program_show", { id: program.id }) }}"> 👀 En savoir plus 👀</a>
        </div>
    {% else %}
        <h2> Aucune série trouvée dans cette catégorie 😭 </h2>
    {% endfor %}
    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}